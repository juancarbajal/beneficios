<?php if ($this->pageCount): ?>
    <div class="box-footer clearfix">
        <ul class="pagination pagination-sm no-margin pull-right">
            <li class="<?= !isset($this->previous) ? 'disabled' : ''; ?>">
                <a href="<?= $this->url('demandas-ofertas', array('action' => 'index', 'page' => $this->first,
                    'order_by' => $order_by, 'order' =>$order , 'q1' => $q1, 'q2' => $q2, 'q3' => $q3)); ?>">&laquo;</a>
            </li>
            <?php foreach ($this->pagesInRange as $page): ?>
                <li class="<?= $page == $this->current ? 'active' : ''; ?>">
                    <a href="<?= $this->url('demandas-ofertas', array('action' => 'index', 'page' => $page,
                        'order_by' => $order_by, 'order' => $order, 'q1' => $q1, 'q2' => $q2, 'q3' => $q3)); ?>"><?= $page; ?></a>
                </li>
            <?php endforeach; ?>
            <li class="<?= !isset($this->next) ? 'disabled' : ''; ?>">
                <a href="<?= $this->url('demandas-ofertas', array('action' => 'index', 'page' => $this->last,
                    'order_by' => $order_by, 'order' => $order, 'q1' => $q1, 'q2' => $q2, 'q3' => $q3)); ?>">&raquo;</a>
            </li>
        </ul>
    </div>
<?php endif; ?>